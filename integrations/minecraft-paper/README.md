# Minecraft Paper dedicated adapter

Real Paper server + NL bridge plugin. Plugin sources live under
[`../minecraft/paper/`](../minecraft/paper/).

Connect: `minecraft://host:25565`

```powershell
powershell -File scripts/nl-game-adapter-validate.ps1 -GameId minecraft-paper
```
