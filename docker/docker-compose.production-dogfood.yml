# Production dogfood — public GA launch stack + real Docker fork provisioner
#
#   powershell -File scripts/nl-production-dogfood-stack-up.ps1 -Validate
#
# See docs/NL_PRODUCTION_DOGFOOD.md
services:
  session-host:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: session-host
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "27020:27020"
      - "27021:27021"
    env_file:
      - production-dogfood-fleet.env
    environment:
      NL_BIND: "0.0.0.0"
      NL_HTTP_PORT: "27020"
      NL_WS_PORT: "27021"
      NL_DATA_ROOT: "/data"
      NL_PUBLIC_BASE_URL: "https://127.0.0.1"
      NL_PUBLIC_MODE: "true"
      NL_HARDENING: "true"
      NL_FLEET_ENABLED: "true"
      NL_FLEET_MAX_CONCURRENT: "128"
      NL_FLEET_MIN_TWITCH_FOLLOWERS: "0"
      NL_FLEET_FORK_CREATE_RATE_PER_MIN: "200"
      NL_FLEET_MAX_FORK_CREATES_PER_HOUR: "9999"
      NL_FLEET_STAGING_DEV: "true"
      NL_FLEET_PRODUCTION_READY: "false"
      NL_FLEET_MOD_RETENTION_DAYS: "730"
      NL_FLEET_RELAY_WS_TEMPLATE: "wss://127.0.0.1:8443/{region}/fork/{session}"
      NL_FLEET_TURN_URI: "turn:127.0.0.1:3478?transport=udp"
      NL_FORK_ORCHESTRATOR_ENABLED: "true"
      NL_FORK_ORCHESTRATOR_MODE: "docker"
      NL_FORK_DOCKER_IMAGE: "nl-fork-hello:latest"
      NL_FORK_DOCKER_HOST: "host.docker.internal"
      NL_FORK_CATALOG_ENABLED: "true"
      NL_FORK_CATALOG_MANIFEST: "/app/samples/fork/catalog.json"
      NL_IDENTITY_ENABLED: "true"
      NL_OWNERSHIP_MODE: "mock"
      NL_SOCIAL_ENABLED: "true"
      NL_SOCIAL_MODE: "mock"
      NL_BETA_ENABLED: "false"
      NL_GA_ENABLED: "true"
      NL_GA_OPEN_SIGNUP: "true"
      NL_GA_REQUIRE_LIVE_IDENTITY: "false"
      NL_GA_ALLOW_MOCK_IDENTITY: "true"
      NL_GA_REQUIRE_PRODUCTION_READY: "false"
      NL_GA_REQUIRE_BETA_DISABLED: "true"
      NL_GA_MIN_CATALOG_GAMES: "3"
      NL_GA_REQUIRED_GAMES: "hello-fork,minecraft,beamng"
      NL_GA_SLA_TIER: "standard"
      NL_LIVE_PRODUCTION_ENABLED: "true"
      NL_LIVE_PRODUCTION_DEV: "false"
      NL_MULTIGAME_PRODUCTION_ENABLED: "true"
      NL_MULTIGAME_REQUIRED_GAMES: "hello-fork,minecraft,beamng"
      NL_PARTNERSHIP_ENABLED: "true"
      NL_PARTNERSHIP_GATE_ADMIT: "1"
      NL_LAUNCH_OPS_ENABLED: "true"
      NL_LAUNCH_OPS_DEV: "false"
      NL_LAUNCH_STATUS_PAGE_ENABLED: "true"
      NL_LAUNCH_LEGAL_VERSION: "2026-08-01"
      NL_LAUNCH_BACKUP_ROOT: "/data/backups"
      NL_PRODUCTION_CUTOVER_ENABLED: "true"
      NL_PRODUCTION_CUTOVER_DEV: "true"
      NL_DISTRIBUTION_ENABLED: "true"
      NL_DISTRIBUTION_DEV: "true"
      NL_DISTRIBUTION_CLIENT_VERSION: "1.0.0"
      NL_DISTRIBUTION_WIN_PACKAGE: "downloads/nl-client-win-x64.zip"
      NL_SCALE_RELIABILITY_ENABLED: "true"
      NL_SCALE_RELIABILITY_DEV: "true"
      NL_SCALE_RELIABILITY_MIN_CONCURRENT: "128"
      NL_SCALE_RELIABILITY_MIN_REGIONS: "3"
      NL_LEGAL_COMPLIANCE_ENABLED: "true"
      NL_LEGAL_COMPLIANCE_DEV: "true"
      NL_LEGAL_COMPLIANCE_MIN_AGE: "13"
      NL_PUBLIC_GA_LAUNCH_ENABLED: "true"
      NL_PUBLIC_GA_LAUNCH_DEV: "true"
      NL_PUBLIC_GA_LAUNCH_VERSION: "2026-08-01"
      NL_PUBLIC_GA_SUPPORT_CONTACT: "support@nexoralive.example"
      NL_PRODUCTION_DOGFOOD_ENABLED: "true"
      NL_PRODUCTION_DOGFOOD_DEV: "true"
      NL_PRODUCTION_DOGFOOD_REQUIRED_GAMES: "hello-fork,minecraft,beamng,rimworld"
      NL_PRODUCTION_DOGFOOD_REQUIRE_MULTIGAME: "true"
    volumes:
      - ./production-dogfood-data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:27020/health"]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 30s

  relay-stub:
    image: caddy:2-alpine
    ports:
      - "8443:8443"
    volumes:
      - ./relay-production/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      session-host:
        condition: service_healthy

  edge:
    image: caddy:2-alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./edge-production/Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      session-host:
        condition: service_healthy

  coturn:
    image: coturn/coturn:latest
    ports:
      - "3478:3478/tcp"
      - "3478:3478/udp"
    command:
      - -n
      - --log-file=stdout
      - --listening-port=3478
      - --fingerprint
      - --lt-cred-mech
      - --user=nl:production-dogfood-turn
      - --realm=nl.dogfood
      - --no-cli
